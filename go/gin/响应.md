```go
package main

import (
	"github.com/gin-gonic/gin"
)

// 响应string

func _string(c *gin.Context) {
	c.String(200, "ck")
}

// 响应json
func _json(c *gin.Context) {
	type Userinfo struct {
		Username string `json:"user_name"`
		Age      int
		Password string
	}
	ck := Userinfo{"ck", 26, "1235435"}

	c.JSON(200, ck)
}

// 重定向

func _redirect(c *gin.Context) {
	c.Redirect(302, "http://www.fengfengzhidao.com")
}

// func main() {
// 	r := gin.Default()
// 	r.StaticFile("1.txt", "static/1.txt")
// 	r.StaticFS("/static", http.Dir("static/"))
// 	r.GET("/", _json)

// 	r.GET("/baidu", _redirect)
// 	r.Run(":8080")
// }


```