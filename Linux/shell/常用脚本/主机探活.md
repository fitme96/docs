一个c段的主机探活
```shell
#!/bin/bash 
# 探测一个C段存活主机 
PREFIX='10.0.0.'
for i in {1..254} do 
# 每隔0.3秒ping一次，一共ping2次，并以1毫秒为单位设置ping的超时时间 
	ping -c 2 -i 0.3 -W 1 ${PREFIX}$i &>/dev/null 
	if [ $? -eq 0 ];then 
		echo "${PREFIX}$i is up"
	fi 
done
```